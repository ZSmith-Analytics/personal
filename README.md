# personal

Zack Smith’s public site lives in `personal_website/`. GitHub Pages deploys that folder via `.github/workflows/pages.yml`.

`resume/` is a Google Docs–ready resume copy, separate from the site.
